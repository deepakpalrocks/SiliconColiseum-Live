# Single key (still works)
GROQ_API_KEY=gsk_your-groq-api-key-here

# Multiple keys - comma separated (rotates round-robin to avoid rate limits)
GROQ_API_KEYS=gsk_key1,gsk_key2,gsk_key3,gsk_key4,gsk_key5

# Models (optional - defaults shown)
TRADE_MODEL=llama-3.3-70b-versatile
SENTIMENT_MODEL=llama-3.1-8b-instant

# Shared wallet private key for executing trades on Arbitrum One
WALLET_PRIVATE_KEY=your-private-key-here

# Arbitrum One RPC URL (use Alchemy/Infura free tier, not public RPC)
RPC_URL=https://arb-mainnet.g.alchemy.com/v2/YOUR_KEY

# Server config
PORT=3001
CRON_INTERVAL_MINUTES=5

# Set to "false" if using external cron (e.g. Render free tier + cron-job.org)
USE_INTERNAL_CRON=true
